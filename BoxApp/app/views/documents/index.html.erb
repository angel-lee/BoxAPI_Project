<!DOCTYPE html>
<html>
<head>
	<title>BOA Loan Portal</title>
</head>

<body>
	<h1>Loan Documents</h1>
	<h4>Please upload the documents needed to process your loan application.</h4>

	<% if !flash[:notice].blank? %>
		<div class = "alert alert-info">
			<%= flash[:notice] %>
		</div>
	<% end %>

	<br />

	<table class = "table table-bordered table-striped">
		<thead>
			<tr>
				<th>Name</th>
				<th>Download Link</th>
				<th>Delete File</th>
			</tr>
		</thead>

		<tbody>
			<% @folder_items.each do |item| %>
				<tr>
					<td><%= link_to item.name %></td>
					<td><%= link_to "Download Document", documents_download_path(@item, :type=> item), method: :get %></td>
					<td><%= button_to "Delete", documents_destroy_path(@item, :type=> item), method: :delete, class:"btn btn-danger"%></td>		
				</tr>
			<% end %>
		</tbody>
	</table>

	<div class="center">
		<%= link_to "Add Document", new_document_path, class: "btn btn-lg btn-primary" %>
	</div>
</body>
</html>